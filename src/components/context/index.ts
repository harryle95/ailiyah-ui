import { styled } from "./factory";
import { Theme, ThemeProvider, useThemeContext } from "./ThemeContext";
import { ITailwindTheme, ITailwindKey } from "./types";

export {
    styled,
    Theme,
    ThemeProvider,
    useThemeContext,
    ITailwindKey,
    ITailwindTheme
}